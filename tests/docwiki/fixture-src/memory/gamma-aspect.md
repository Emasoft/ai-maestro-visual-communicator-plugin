---
name: gamma-aspect
description: a general rule shared by many elements
tier: aspect
type: feedback
---

A general aspect rule that radiates down to the elements it governs.

## Applies to

It applies to [[alpha-hub]] and to [[beta-component]].

Also references a [[missing-note|note with explicit label]] form — this target is
dangling on purpose, to prove the label-form not-in-set path renders the LABEL as
plain text, not a broken link.

## Notes and lessons learned
