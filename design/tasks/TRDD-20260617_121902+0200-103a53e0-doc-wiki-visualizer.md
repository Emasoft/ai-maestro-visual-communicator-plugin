---
trdd-id: 103a53e0-fce6-4d7e-9a94-c1e93953a25a
title: Navigable document wiki — visualizer for TRDD / PRRD / Kanban / wikimem
column: dev
created: 2026-06-17T12:19:02+0200
updated: 2026-06-17T13:17:24+0200
current-owner: amvcp-claude
assignee: amvcp-claude
priority: 2
severity: MEDIUM
effort: XL
labels: [visualizer, navigation, docs, wiki]
task-type: feature
parent-trdd: null
npt: []
eht: []
relevant-rules: []
release-via: publish
delivery: pull-request
target-branch: main
feature-branch: feat/doc-wiki-visualizer
test-requirements: [dev-browser-headless, lint]
review-requirements: [human-review]
runtime-targets: [macos, linux]
impacts: []
attempts: 0
last-test-result: pass
implementation-commits: [1986bc5, 3aede10, f00bc26, 54b5597]
---

# Navigable document wiki — TRDD / PRRD / Kanban / wikimem visualizer

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative) — 2026-06-17

**Goal (user, verbatim intent):** render the 4 AI-Maestro artifact types — TRDD,
PRRD, Kanban, wikimem-memory markdown — as ONE interactive HTML "wiki" where
cross-file links + reference-paths are CLICKABLE and NAVIGATE to the corresponding
rendered file. Browser-style back/forward history. Permanent breadcrumb bar on top.
Wikipedia-style navigation. A SEARCH element: user types keywords → Claude runs
**memgrep** → a SCRIPT generates the Wikipedia-style results-list HTML (Claude
spends ≈0 context tokens).

**Branch / worktree:** `feat/doc-wiki-visualizer` off `main` (f348b5d), worktree
`/tmp/amvcp-docwiki`. Independent of the remediation branch.

**ARCHITECTURE DECISION (made — single self-contained hash-routed SPA):**
- ONE self-contained `.html` per doc-set that **just opens** (file://) — honors the
  project "Compilable = one self-contained .html" facet. All rendered docs embedded.
- **Hash-routing** (`#/trdd/<id>`, `onhashchange`) — robust on file:// (History API
  pushState can't change path on file://); the browser's hash history gives
  **back/forward for free**; in-page back/fwd buttons call `history.back/forward()`.
- Per-doc "pages" are sections swapped on navigation; breadcrumb + active-page state
  update per route. Page EXPANDS (no nested scrollbars — project rule).
- Fixed interaction model UNCHANGED (select/hover/triple-state/comment via runtime);
  the wiki nav is ADDED chrome, not a replacement.

**PHASE 1 — DONE** (commit `1986bc5`): `scripts/amvcp-docwiki.js` + `tests/docwiki/phase1-fixture.html`.
Dev-browser verified: hash-route/page-swap, breadcrumb trail (grow/dedup/truncate),
native back/forward, search box, light+dark screenshots clean
(`reports/screenshots/docwiki-phase1-trdd-{light,dark}.png`).

**PHASE 2 — DONE** (commit `3aede10`): `scripts/amvcp-docwiki-build.py` (stdlib-only) +
`tests/docwiki/fixture-src/`. Renders TRDD + PRRD: frontmatter card + pure-python
markdown→HTML (headings/tables/lists/code-fences/inline/bold/links, HTML-escaped),
cross-links `TRDD-<8hex>`→`#/trdd` + `relevant-rules`/`PRRD G/S<n>`→`#/prrd#G<n>`,
emits ONE self-contained SPA inlining the phase-1 shell. Verified: 21 real TRDDs →
21 pages, 0 dangling; home grouped by column; nav lands correctly; 32 `<pre>` blocks;
PRRD path via fixture (0 dangling rule links); light+dark clean
(`reports/screenshots/docwiki-phase2-trdd-{light,dark}.png`); pyright/py_compile clean.

**PHASE 3 — DONE** (commit `f00bc26`): Kanban board (`#/kanban`, 14-stage lanes of
clickable TRDD cards) + wikimem (`--mem-dir`, `#/mem/<name>` pages, `[[name]]`/
`[[name|label]]` → `#/mem` links with label shown, out-of-set → "(not in set)"). +
`tests/docwiki/fixture-src/memory/`. Verified on real 21 TRDDs + 3-note fixture: 2
lanes/21 cards, 3 mem pages, all wikilinks resolve (0 dangling), deliberate dangling →
plain text; no p2 regression; light+dark clean
(`reports/screenshots/docwiki-phase3-{kanban-light,kanban-dark,mem-light}.png`).

**PHASE 4 — DONE** (commit `54b5597`): search. CLIENT-SIDE instant search — the shell
renders a Wikipedia-style results list on `#/search?q=` by matching terms (AND, title>body
rank) against the ALREADY-EMBEDDED section text (no separate index, self-contained); box
prefills from `q`; build adds the empty `data-ve-doc=search` section. PLUS
`scripts/amvcp-docwiki-search.py` (stdlib): memgrep `--json`/plain-paths → same
results-fragment markup, path→route mapping (PRRD→prrd, TRDD→trdd/<8hex>, *.md→mem/<stem>,
unknown→plain row) for the agent flow. Verified: q=audit→12, q=theme→14, links route,
search.py maps real+bogus correctly, no regression; light+dark clean
(`reports/screenshots/docwiki-phase4-search-{light,dark}.png`).

**NEXT ACTION:** Phase 5 (FINAL) — package as a plugin skill.
1. `skills/amvcp-doc-wiki/SKILL.md` (+ `references/` if needed): document the 4 render
   mappings, the build CLI (`amvcp-docwiki-build.py OUT.html --trdd-dir … --mem-dir … --prrd …`),
   the search flow (client-side + the `memgrep … | amvcp-docwiki-search.py` agent recipe),
   the self-contained-SPA + hash-route contract, light+dark, the FIXED interaction model,
   and the 6 facets (Editable via comment round-trip · Commentable · Compilable ·
   Stylizable · Pickable · Exportable) per CLAUDE.md §3. Frontmatter `description` ≤ ~200
   tok with trigger phrases ('visualize TRDD', 'render the kanban', 'browse the design
   docs', 'doc wiki', 'navigable PRRD', …). SKILL.md body ≤ ~5000 tok / 500 lines.
2. Wire into the umbrella `skills/amvcp-visual-communication/SKILL.md`: add to the
   supporting-skills paragraph + a decision-matrix row ("multi-file TRDD/PRRD/kanban/wikimem
   with cross-file links + browsing" → amvcp-doc-wiki).
3. `tests/docwiki/` runner + README; run CPV on the new skill (expect only the known
   CPV-FP wall). Screenshot the home index light+dark. Then column→complete; PR for owner.

**Load-bearing facts (from groundwork map, Explore agent 2026-06-17):**
- REUSE (one-skill-per-thing — do NOT duplicate): `amvcp-prose-pages` (markdown
  body render), `amvcp-tables` (frontmatter/tabular), `amvcp-interactive-controls`
  (kanban drag-reorder + `references/breadcrumb-stack.md` scaffold), `amvcp-modal-comments`.
- Runtime nav primitives that EXIST: `location.search/origin`, `location.replace()`
  (one-shot), `window.veSelection[]` + `toggleElementSelection()` (runtime ~line 2606),
  `[data-ve-id]` click handler (~2625). **NO `<a>` interception, NO hash-routing, NO
  history, NO breadcrumb — all must be built.**
- Interaction hooks: `window.veSelect({id,type,label,data})`; `data-ve-id`/`data-ve-type`
  stamps; theming via `scripts/amvcp-designmd.js` `resolveTokens()`/`applyTokens()`,
  `--vc-*` vars only (zero hardcoded palette; light + dark both).
- Umbrella wiring: `skills/amvcp-visual-communication/SKILL.md` — add the new skill to
  the **supporting-skills** paragraph (~lines 57-58) + a decision-matrix row
  (~lines 59-109). It is a COORDINATOR/router, not a 13-category generic primitive.
- No prior TRDD for this; `visualizing-triage` reports are an unrelated corpus triage.

**SUPERSEDED / do NOT carry forward:** nothing yet.

**Durable artifacts to read before acting:** this TRDD; the groundwork map is
captured above (the Explore agent's report is ephemeral).

---

## The 4 things and their cross-link semantics

| Thing | Source | Render via | Clickable cross-links |
|---|---|---|---|
| **TRDD** | `design/tasks/TRDD-*.md` (YAML frontmatter + body) | frontmatter card (`amvcp-tables`) + body (`amvcp-prose-pages`) | `npt:` / `eht:` / `blocked-by:` / `parent-trdd:` / `supersedes:` / `superseded-by:` → target TRDD page; `PRRD G/S<n>.<v>` citations → PRRD rule anchor; `implementation-commits:` → (optional) GitHub |
| **PRRD** | `design/requirements/PRRD.md` (G/S rules) | `amvcp-prose-pages` + anchored rule list (`#G64`, `#S70`) | incoming from TRDD citations; `[[…]]`/`See PRRD …` → rule anchors |
| **Kanban** | derived: scan `design/tasks/` frontmatter `column:` | `amvcp-interactive-controls` kanban (14-stage pipeline) | each card → its TRDD page |
| **wikimem** | memory dir `*.md` (frontmatter `name`/`description`/`tier`/`type` + `[[name]]` body links) | `amvcp-prose-pages` + frontmatter strip | `[[name]]` wikilinks → target memory page; `Applies to`/`Governed by`/`See also` rays |

## The navigation shell (the shared layer — Phase 1)

`scripts/amvcp-docwiki.js`:
- **Hash-router**: `#/<type>/<id>` → show that page, hide others, update breadcrumb +
  active state. `onhashchange` drives it; initial load routes to `#/home` (an index).
- **Link interception**: any `[data-ve-navigate="<type>/<id>"]` (or in-wiki `<a>` whose
  href is an in-set doc) → `location.hash = …` (lets the browser record history).
- **Back/forward**: native (hash history). In-page ◀ ▶ buttons call `history.back()` /
  `history.forward()`. A **breadcrumb bar** (top, sticky) shows Home › … › current,
  each crumb clickable; reuse `breadcrumb-stack.md` styling.
- **Search box** (top bar): submits the query; the SPA shows a `#/search?q=…` results
  page whose HTML was pre-generated by the search script (Phase 4) and embedded, OR
  (live mode) injected by the agent after a memgrep run.
- All chrome themed from `--vc-*` (light + dark), `overflow:visible` (no inner scroll).

## Build pipeline + search (Phases 2-4)

- `scripts/amvcp-docwiki-build.py` — scan the target folders, parse frontmatter +
  cross-refs, render each doc to an embedded page, build the link graph + breadcrumb
  ancestry + the kanban index, emit ONE self-contained `<docset>.html` (runtime + CSS
  inlined). Fail-fast on a broken cross-ref (report it, don't silently drop).
- `scripts/amvcp-docwiki-search.py` — stdin = `memgrep recall --json` (or plain
  grep) output; stdout = the Wikipedia-style results-list HTML fragment (title +
  description snippet + `data-ve-navigate` link per hit). Claude pipes memgrep → this
  → gets HTML, ≈0 context tokens.

## Coordinator skill (Phase 5)

`skills/amvcp-doc-wiki/SKILL.md` — documents: the 4 render mappings, the nav model,
the build + search commands, the self-contained-SPA contract, light+dark, the fixed
interaction model. Wire into the umbrella (supporting-skills + decision-matrix row).
Every facet (Editable via comment round-trip · Commentable · Compilable ·
Stylizable · Pickable · Exportable) per project CLAUDE.md §3.

## Phases (each ≤5 files, verify between)

1. **Nav shell** — `scripts/amvcp-docwiki.js` + a hand-authored SPA fixture; dev-browser
   screenshot light+dark (hash-route swap, breadcrumb, back/forward, link-intercept).
2. **Build pipeline + TRDD + PRRD renderers** — `amvcp-docwiki-build.py`; render real
   `design/tasks/` + `PRRD.md`; verify cross-links resolve.
3. **Kanban + wikimem renderers** — kanban index from `column:`; wikimem `[[…]]` links.
4. **Search** — `amvcp-docwiki-search.py` + memgrep integration + the shell search UI.
5. **Coordinator skill + umbrella wiring + tests** — `tests/` runner, screenshots,
   CPV-clean the new skill.

## Acceptance criteria

- Opening the emitted `.html` shows a home/index; clicking any cross-link navigates to
  the target doc page; browser + in-page back/forward work; breadcrumb tracks the trail.
- TRDD `npt/eht/blocked-by/parent` + `PRRD G/S` citations + wikimem `[[…]]` are all
  clickable and land on the right page. Kanban cards link to TRDDs.
- Search: `memgrep recall "q" <dir> | amvcp-docwiki-search.py` emits a results page the
  wiki can show; clicking a result navigates to that doc.
- Light + dark both correct (screenshot-verified); no nested scrollbars; self-contained
  single file opens via file://. New skill passes CPV (modulo the known CPV-FP wall).
